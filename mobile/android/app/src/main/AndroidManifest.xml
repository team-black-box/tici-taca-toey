<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        <!-- The custom scheme carries every URL the app answers to; the
             shapes themselves live in one place, App.tsx's `linking`. -->
        <intent-filter>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.DEFAULT" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="ticitacatoey" />
        </intent-filter>
        <!-- App Links for the real domain: only the paths the web also
             serves, so a link means the same thing either way. autoVerify
             needs /.well-known/assetlinks.json with the release-signing
             cert fingerprint, served at production cutover. -->
        <intent-filter android:autoVerify="true">
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.DEFAULT" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="https" />
            <data android:host="ticitacatoey.com" />
            <data android:host="www.ticitacatoey.com" />
            <data android:path="/" />
            <data android:path="/daily" />
            <data android:path="/leaderboard" />
            <data android:pathPrefix="/play/" />
            <data android:pathPrefix="/spectate/" />
            <data android:pathPrefix="/player/" />
            <data android:pathPrefix="/replay/" />
        </intent-filter>
      </activity>
    </application>
</manifest>
