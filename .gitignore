# dependencies
node_modules/

# build output
dist/

# logs
*.log

# environment
.env
.env.*

# os / editor
.DS_Store

# typescript cache
*.tsbuildinfo

# TTN game data log
server/data/

# The archive database. Run from the repo root (as .claude/launch.json
# does) the server writes it next to the committed TTN corpus, and unlike
# a notation line a database holds handles - it must never be committed.
data/*.db
data/*.db-shm
data/*.db-wal

# generated by playground/train.ts
playground/policy.json
.vercel/
